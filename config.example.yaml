server:
  listen: ":8080"
storage:
  path: data/triage.db
correlation:
  window: 15m
  max_incident_age: 6h
  suppressions_file: configs/suppressions.example.yaml
triage:
  llm_threshold: 40
  mode: rule-only
