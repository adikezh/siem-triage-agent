# Delivery backlog

- [~] SQLite WAL store saves normalized alerts and incidents, with source cursor, idempotent outbox, retry/backoff dispatcher, configurable alert/LLM retention pruning, and optional live webhook sender wiring.
- [~] Wazuh/OpenSearch-compatible search-after client, generic Elastic/OpenSearch field-mapped client, optional continuous `serve --source-url` polling, page/cross-page alert→incident correlation with configurable grouping overrides, source→process→cursor→outbox worker, and optional notifications are present; external source proof remains.
- [~] YAML/CSV asset inventory and local IOC enrichment are wired into scoring; opt-in AbuseIPDB/VirusTotal IP clients have 5–6s timeouts and a SQLite-backed 24h cache, local MaxMind GeoLite2 country lookup is available for LLM context, and the last five same-fingerprint incidents/verdicts are available; broader agent/src history remains.
- [~] YAML config and suppression decisions are wired into offline and live ingest; DB-backed suppression CRUD/API, created-by/created-at audit fields, and tamper-evident audit hashing are present; richer field matching in live source policy remains.
- [x] Deterministic scoring formula, table tests, feedback FP-history deduction, JSONL feedback export and eval precision/recall command are present.
- [x] Scenario fixtures for brute-force, lateral movement, and FIM noise are checked in CI for expected incident counts and severity.
- [~] PII redaction, prompt builder, OpenAI-compatible/Ollama/Anthropic providers, strict output/action validation, configured provider fallback, persistent budget guard, triage merge engine, versioned prompts, config-selected providers, CLI opt-in runtime path and LLM trace schema are present.
- [~] Generic HMAC webhook, Telegram Bot API, Slack Block Kit, Jira REST, IRIS and TheHive senders plus Wazuh field normalizer are present and tested; Telegram/Slack callback endpoints persist TP/FP/Ack feedback, native signature verification and TP/FP/Ack/Open button cards are available.
- [~] Optional API-key Bearer authentication supports env keys and hashed SQLite keys with create/list/revoke/rotate CLI lifecycle; viewer/analyst/admin roles are enforced on read, feedback, suppression create and suppression delete routes, API and dashboard support severity/since/limit filters, configured assets are exposed read-only, and protected APIs have per-client rate limiting. Markdown/PDF/DOCX reports work; broader RBAC policy remains.
- [~] Hosted CI, Docker build, Compose writable SQLite volume, self-contained demo loader/server, container `/health`, Helm chart, GoReleaser release workflow, tag `v0.1.3` publication with archives/deb/rpm/GHCR/SBOM, Prometheus incident/LLM/feedback/outbox metrics, and a GitHub Pages product page workflow are present; real SOC demo evidence, live-provider evidence, and production rollout gates remain.
